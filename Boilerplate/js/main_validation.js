/*
var Song = Backbone.Model.extend({
    validate: function(attrs) {
        if (!attrs.title)
            return "Title is required";
    }
});

var song = new Song();
*/

//use the isValid method in the console to check whether or not an element is valid
//check the last string of errors by song.validationError to see what the error was